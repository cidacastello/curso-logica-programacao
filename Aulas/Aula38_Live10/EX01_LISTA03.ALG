Algoritmo "Ex01_Lista03"

Var

   i, soma : inteiro

Inicio

      //inicialização
      //incremento
      //parada
     para i de 0 até 20 faça
        se i MOD 2 <> 0 então
           soma <- soma + i
        fimse
     fimpara
     
     escreval("Soma = ", soma)

Fimalgoritmo